{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "echo 'Skipping preinstall check'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:web": "pnpm run typecheck && pnpm --filter @workspace/apa-medical run build && pnpm --filter @workspace/api-server run build",
    "dev:client": "pnpm --filter @workspace/apa-medical run dev",
    "dev:server": "pnpm --filter @workspace/api-server run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "start:web": "pnpm --filter @workspace/api-server run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "vercel": "^52.0.0"
  }
}
